# Driver: argument parsing, help text, and exit codes. The version header is
# generated by the top-level CMakeLists (cmake/version.h.in) and reaches this
# target through minc_target_configure().
minc_add_library(minc_driver SOURCES cli.cc help_text.cc)

minc_add_executable(mincc SOURCES main.cc DEPS minc_driver)
